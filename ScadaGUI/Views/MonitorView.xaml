<UserControl x:Class="ScadaGUI.Views.MonitorView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:converters="clr-namespace:ScadaGUI.Converters"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">

    <UserControl.Resources>
        <converters:BooleanToVisibilityConverter x:Key="BoolToVis"/>
    </UserControl.Resources>

    <Grid Margin="10">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <GroupBox Grid.Column="0" Header="Tag Values">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <DataGrid Grid.Row="0" ItemsSource="{Binding MonitoredTags}" 
                          SelectedItem="{Binding SelectedTag}"
                          AutoGenerateColumns="False" 
                          CanUserAddRows="False"
                          IsReadOnly="True">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Name" Binding="{Binding Name}" Width="*" />
                        <DataGridTextColumn Header="Value" Binding="{Binding CurrentValue, StringFormat={}{0:F2}}" Width="Auto"/>
                        <DataGridTextColumn Header="Units" Binding="{Binding Units}" Width="Auto"/>

                        <DataGridCheckBoxColumn Header="Scan On" Binding="{Binding IsScanning}" Width="Auto"/>

                    </DataGrid.Columns>
                </DataGrid>

                <StackPanel Grid.Row="1" Margin="0,10,0,0"
                            Visibility="{Binding IsOutputTagSelected, Converter={StaticResource BoolToVis}}">
                    <TextBlock Text="{Binding SelectedTag.Name, StringFormat='Write new value for: {0}'}" FontWeight="Bold"/>
                    <StackPanel Orientation="Horizontal" Margin="0,5,0,0">
                        <Label Content="New Value:" VerticalAlignment="Center"/>
                        <TextBox Width="150" Margin="5,0,0,0" VerticalAlignment="Center"
                                 Text="{Binding ValueToWrite, UpdateSourceTrigger=PropertyChanged}"/>
                        <Button Content="Write to Tag" Margin="10,0,0,0" Padding="10, 5"
                                Command="{Binding WriteToTagCommand}"/>
                    </StackPanel>
                </StackPanel>
            </Grid>
        </GroupBox>

        <GroupBox Grid.Column="1" Header="Active Alarms" Margin="10,0,0,0">
            <DataGrid ItemsSource="{Binding ActiveAlarms}" AutoGenerateColumns="False" IsReadOnly="True">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Time" Binding="{Binding Time, StringFormat={}{0:dd.MM.yyyy HH:mm:ss}}" Width="Auto"/>
                    <DataGridTextColumn Header="Tag Name" Binding="{Binding TagName}" Width="Auto"/>
                    <DataGridTextColumn Header="Message" Binding="{Binding Message}" Width="*"/>
                </DataGrid.Columns>
            </DataGrid>
        </GroupBox>
    </Grid>
</UserControl>